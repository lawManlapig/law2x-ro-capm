_schema-version: 3.3.0
ID: law2x-ro-equip-inventory
description: A simple CAP project.
version: 1.0.0
modules:
- name: law2x-ro-equip-inventory-srv
  type: nodejs
  path: gen/srv
  requires:
  - name: law2x-ro-equip-inventory-auth
  - name: law2x-ro-equip-inventory-db
  provides:
  - name: srv-api
    properties:
      srv-url: ${default-url}
  parameters:
    buildpack: nodejs_buildpack
    instances: 1
  build-parameters:
    builder: npm-ci
- name: law2x-ro-equip-inventory-db-deployer
  type: hdb
  path: gen/db
  requires:
  - name: law2x-ro-equip-inventory-db
  parameters:
    buildpack: nodejs_buildpack
- name: law2x-ro-equip-inventory-destination-content
  type: com.sap.application.content
  requires:
  - name: law2x-ro-equip-inventory-destination-service
    parameters:
      content-target: true
  - name: law2x-ro-equip-inventory_html_repo_host
    parameters:
      service-key:
        name: law2x-ro-equip-inventory_html_repo_host-key
  - name: law2x-ro-equip-inventory-auth
    parameters:
      service-key:
        name: law2x-ro-equip-inventory-auth-key
  parameters:
    content:
      instance:
        destinations:
        - Name: ROAppRouter_law2x_ro_equip_inventory_html_repo_host
          ServiceInstanceName: law2x-ro-equip-inventory-html5-app-host-service
          ServiceKeyName: law2x-ro-equip-inventory_html_repo_host-key
          sap.cloud.service: ROAppRouter
        - Authentication: OAuth2UserTokenExchange
          Name: ROAppRouter_law2x_ro_equip_inventory_auth
          ServiceInstanceName: law2x-ro-equip-inventory-auth
          ServiceKeyName: law2x-ro-equip-inventory-auth-key
          sap.cloud.service: ROAppRouter
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: law2x-ro-equip-inventory-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: law2x-ro-equip-inventory_html_repo_host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - eqinv.zip
      name: eqinv
      target-path: resources/
- name: eqinv
  type: html5
  path: app/eq-inv
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: law2x-ro-equip-inventory-auth
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      oauth2-configuration:
        credential-types:
        - binding-secret
        - x509
      tenant-mode: dedicated
      xsappname: law2x-ro-equip-inventory-${space}
    path: ./xs-security.json
    service: xsuaa
    service-plan: application
- name: law2x-ro-equip-inventory-db
  type: com.sap.xs.hdi-container
  parameters:
    service: hana
    service-plan: hdi-shared
- name: law2x-ro-equip-inventory-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          - Authentication: NoAuthentication
            HTML5.DynamicDestination: true
            HTML5.ForwardAuthToken: true
            Name: srv-api
            ProxyType: Internet
            Type: HTTP
            URL: ~{srv-api/srv-url}
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: law2x-ro-equip-inventory-destination-service
    service-plan: lite
  requires:
  - name: srv-api
- name: law2x-ro-equip-inventory_html_repo_host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: law2x-ro-equip-inventory-html5-app-host-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
build-parameters:
  before-all:
  - builder: custom
    commands:
    - npm ci
    - npx cds build --production
